# my-skills

My reusable agent skills.

## Install

Install all skills interactively:

```sh
npx skills add okharedia/my-skills
```

Install `tldr` globally for Codex:

```sh
npx skills add okharedia/my-skills --skill tldr --agent codex --global
```

Omit `--global` to install it only in the current project.
